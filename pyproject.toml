[build-system]
requires = []
build-backend = "setuptools.build_meta"

[project]
name = "python-contextvar-executor-debug-lab"
version = "0.1.0"
description = "ContextVar と asyncio のスレッド境界を再現するデバッグ教材"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-ra"
